{
  "name": "pr-auto-ready",
  "version": "1.0.1",
  "description": "A CLI tool to automatically mark GitHub PRs as ready for review when all checks pass",
  "type": "module",
  "main": "./bin/pr-auto-ready",
  "bin": {
    "pr-auto-ready": "./bin/pr-auto-ready",
    "prar": "./bin/pr-auto-ready"
  },
  "scripts": {
    "build": "tsc && mv bin/pr-auto-ready.js bin/pr-auto-ready && chmod +x bin/pr-auto-ready"
  },
  "keywords": [
    "github",
    "pr",
    "automation",
    "cli"
  ],
  "author": "syumai",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.18.6",
    "typescript": "^5.9.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
